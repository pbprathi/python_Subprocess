{
  "NANO": {
      "CHECK":"dpkg -s nano",
    "INSTALL": "sudo apt-get install nano",
    "REMOVE": "sudo apt-get remove nano"
  },
  "PYTHON": {
      "CHECK":"dpkg -s python3",
    "INSTALL": "sudo apt-get install python3",
    "REMOVE": "sudo apt-get remove python3"
  },

  "CI": {
    "SERVICE": "travis-ci",
    "HOOK_URL": "web-hooking-url-from-ci-service"
  }
}
